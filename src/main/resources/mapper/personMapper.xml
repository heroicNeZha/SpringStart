<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace：该映射文件唯一标识-->
<mapper namespace="edu.ustc.SpringStart.person.mapper.PersonMapper">

    <sql id="selectAll">
        select * from person
    </sql>

    <resultMap id="personResult" type="person">
        <!-- 分为主键和非主键-->
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
        <result property="sex" column="sex" javaType="Boolean" jdbcType="INTEGER"/>
    </resultMap>

    <select id="queryPersonByPerson" resultMap="personResult" parameterType="person">
        <include refid="selectAll"/>
        <where>
            <if test="age!=null and id!=0">
                and id = #{id}
            </if>
            <if test="name!=null and name != '' ">
                and name like '%${name}%'
            </if>
            <if test="age!=null and age != 0 ">
                and age = #{age}
            </if>
            <if test="sex!=null and sex != '' ">
                and sex = #{sex,javaType=boolean,jdbcType=INTEGER}
            </if>
        </where>
    </select>

    <select id="queryPersonByAge" resultMap="personResult" parameterType="HashMap" statementType="CALLABLE">
        {
            CALL queryPersonByIdWithProcedure(#{start_age,jdbcType=INTEGER,mode=IN},
                                              #{end_age,jdbcType=INTEGER,mode=IN}, #{p_count,jdbcType=INTEGER,mode=OUT})
            }
    </select>

    <insert id="addPerson" parameterType="person">
        insert into person(name, age, sex)
        values (#{name}, #{age}, #{sex,javaType=boolean,jdbcType=INTEGER})
    </insert>

    <update id="updatePersonById" parameterType="person">
        update person
        set name = #{name},
            age  = #{age},
            sex  = #{sex,javaType=boolean,jdbcType=INTEGER}
        where id = #{id}
    </update>

    <delete id="deletePersonById" parameterType="int">
        delete
        FROM person
        where id = #{id}
    </delete>

</mapper>